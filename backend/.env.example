# PostgreSQL connection string for backend/app/db.py (SQLAlchemy).
# Format: postgresql+psycopg2://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=postgresql+psycopg2://mindaura:mindaura_dev_pw@127.0.0.1:5432/mindaura

# Used only by tests/unit/test_video_analysis_db.py, so tests never touch
# the database above.
TEST_DATABASE_URL=postgresql+psycopg2://mindaura:mindaura_dev_pw@127.0.0.1:5432/mindaura_test
